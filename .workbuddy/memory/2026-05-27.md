# 2026-05-27

## 准备上传 GitHub + GitHub Actions 打包 APK

### 代码修复
1. **main.py**：删除 CNLabel/CNButton 重复的裸类定义（各定义了两次，第二次覆盖第一次），保留带 `__init__` 的版本
2. **config.py**：API Key 改为从环境变量读取（`os.environ.get`），不再硬编码，本地可用 .env 文件，GitHub Actions 用 Secrets 注入

### buildozer.spec 修复
- `source.include_exts` 加入 `ttf`，确保中文字体被打包进 APK
- 开启 `android.permissions = INTERNET`（调用 LLM API 和天气接口必须有）
- 取消注释 `android.api = 33` 和 `android.minapi = 21`
- 开启 `android.accept_sdk_license = True`（无人值守打包必须）

### 新增文件
- `.github/workflows/build-apk.yml`：完整的 GitHub Actions 流程，含字体下载、依赖安装、缓存、APK 打包、产物上传
- `.gitignore`：排除 `.buildozer/`、`bin/`、`__pycache__/`、`.env` 等
